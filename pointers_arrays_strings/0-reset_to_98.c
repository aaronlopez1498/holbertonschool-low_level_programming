#include "main.h"
#include <stdio.h>

/**
 * reset_to_98 - write a function that takes a pointer to an
 * int as parameter and updates the value it points to 98.
 * @n: The pointer to an int
 */
void reset_to_98(int *n);
{
	*n = 98;
}
